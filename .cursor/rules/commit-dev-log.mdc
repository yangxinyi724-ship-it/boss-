---
description: 提交时把改动/新功能/修错写入 docs/dev-log.md
alwaysApply: true
---

# 提交时写开发记录

当用户要求 **创建 git commit**（或说「提交并记一下改了什么」）时：

1. 按既有 Git 安全协议做 status / diff / log，再提交。
2. **额外**：根据本次 diff，在 `docs/dev-log.md` **最上方**（第一条 `---` 下面）追加 **一条** 中文记录（改动很大可同日多条）。
3. 条目包含实际有的字段（没有就省略）：
   - `## YYYY-MM-DD — 简短标题`
   - **类型**：功能 / 修复 / 改动 / 文档·测试
   - **改动**：改了什么行为或模块
   - **新增**：新功能或新入口
   - **问题** / **原因** / **处理**：若本次含修错
4. **要记**：用户可感知的行为变化、新功能、bug 修复、重要配置/数据路径变更。  
   **可不记**：纯格式、无行为变化的重构、只动测试且无产品含义的琐碎调整。
5. **不要**在 `docs/dev-log.md` 里写「怎么写」、格式模板、示例代码块等说明书内容；文件里只留真实条目。
6. 将 `docs/dev-log.md` **纳入同一次 commit**。
7. commit message 仍以 why 为主；正文可简列 `Changes:` / `Fixes:` 要点。

若仓库里还有旧的 `docs/bugfixes.md`，新内容只写 `docs/dev-log.md`，不要两边重复记。

不要自动 commit，除非用户明确要求提交。
